import SidebarList from './SidebarList';

const Sidebar = () => {
  return (
    <nav>
      {/* Logo */}
      {/* Links to different pages map */}
      <SidebarList />
      {/* profile */}
    </nav>
  );
};

export default Sidebar;
